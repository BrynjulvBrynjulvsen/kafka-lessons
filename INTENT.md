# Kafka lessons intent

Teach partitioning, ordering, consumer groups, offsets/replay and lag through
predict → run → observe → inspect code → change one thing. Keep experiment loops
readable and presenter commands explicit. Reuse the Kafka core without Kubernetes
dependencies. New lessons and experiment-specific features belong here.
